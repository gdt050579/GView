[package]
name = "gview-generic-hashes"
description = "GView generic plugin: streaming hash calculator (Hashes / ComputeMD5 / ComputeSHA256 commands)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
appcui = { path = "../../../../AppCUI-rs/appcui" }
gview-core = { path = "../../../crates/gview-core" }
gview-hashes = { path = "../../../crates/gview-hashes" }
gview-plugin = { path = "../../../crates/gview-plugin" }
