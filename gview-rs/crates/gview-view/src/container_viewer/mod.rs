//! `ContainerViewer` (archive/VFS tree) — C++ `ContainerViewer/`.
//!
//! [`tree`] holds the lazy `PopulateItem` tree model; open/extract
//! flows arrive with `container-open-extract`.

pub mod tree;
